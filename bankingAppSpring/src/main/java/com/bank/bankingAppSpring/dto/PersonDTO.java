package com.bank.bankingAppSpring.dto;

public record PersonDTO(String id,
                        String firstName,
                        String secondName,
                        String username,
                        String password
                        ) {
}